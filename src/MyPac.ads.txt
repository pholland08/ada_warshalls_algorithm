
with Ada.Text_IO; use Ada.Text_IO;
with Ada.Strings.Unbounded; use Ada.Strings.Unbounded;
with Ada.Text_IO.Unbounded_IO; use Ada.Text_IO.Unbounded_IO;


package MyPac is


   type intArray is array(Positive range <>) of Integer;
   type intMatrix is array(Positive range <>, Positive range <>) of Integer
     with Default_Component_Value => 0;
--   type myArray is array(Positive range <>) of myType;
   type myArrayOfStrings is array(Positive range <>) of Ada.Strings.Unbounded.Unbounded_String;


   procedure printArrayOfStrings(arr : in myArrayOfStrings);
   procedure printArrayOfIntegers(arr : in intArray);
   procedure printMatrixOfIntegers(arr : in intMatrix; int : in Integer);

end MyPac;

